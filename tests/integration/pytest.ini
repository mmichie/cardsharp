[pytest]
asyncio_mode = auto
log_cli = true
log_cli_level = INFO
# Hang guard (see root pytest.ini); the old timeout = 5 here predates
# the pytest-timeout plugin being installed and was never enforced.
timeout = 1800
timeout_method = thread
testpaths = tests/integration