[pytest]
asyncio_mode = auto
log_cli = true
log_cli_level = INFO
# Hang guard (see root pytest.ini): bounded, named failures instead of
# silent deadlocks; ceiling clears the slowest legitimate test on CI.
timeout = 1800
timeout_method = thread
